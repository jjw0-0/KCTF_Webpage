/**
 * PostCSS 설정 파일
 * CSS를 변환하는 도구(플러그인)들을 정의하며, Tailwind CSS를 처리합니다.
 *
 * @type {import('postcss-load-config').Config}
 */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;
